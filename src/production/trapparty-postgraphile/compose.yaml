services:
  trapparty-postgraphile:
    deploy:
      labels:
        - (( append ))
        - traefik.http.routers.trapparty-postgraphile-secure.middlewares=cloudflarewarp,header_cors
        - traefik.http.routers.trapparty-postgraphile-secure.tls.certresolver=default
      update_config:
        order: start-first
